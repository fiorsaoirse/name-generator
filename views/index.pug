extends layout

block content
    script(type="text/javascript").
      function reset() {
        const textarea = document.getElementById('search');
        textarea.value = '';
      }

    div(class='container')
        div(class='row justify-content-md-center')
            div(class='col')
                form(action='/words' method='post' class='w-100')
                    label(for='search' class='mb-1') Введите слоги через запятую:
                    div(class='input-group')
                        div(class='input-group-prepend')
                            span(class='input-group-text icon')
                                span(class='close-icon pointer' onclick='reset()')
                        textarea(
                            id='search'
                            name='data'
                            class='form-control'
                        )
                    button(
                        class='btn btn-outline-secondary mt-2'
                        type='submit'
                    ) Отправить